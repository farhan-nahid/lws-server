const todos = require("./data/todos");
const tags = require("./data/tags");
const videos = require("./data/db_videos");
const transactions = require("./data/transactions");

module.exports = {
  todos,
  videos,
  tags,
  transactions,
};
